---
base: "[[IMG-20260321214755797.base]]"
태그: []
---
- 왜 testserver를 파게 되었는가
- 왜 로깅을 하게 되었는가 (특히 서버 에러)
- sqlmodel이 아닌 sqlalchemy를 쓰게 된 이유
- 인증방식을 supabase에서 떼와서 그대로 쓰게 된 이유
- placename &amp; 처리 문제. 뒤늦게 처리하다가 pk 충돌 나서 self join으로 해결한
- webhook 을 분리하는 문제
    - 여기서 발생한 sqlalchemy문제. 신버전 sqlalchemy 모델을 DB에 적용시켜놓은 상태에서 구버전 sqlalchemy 모델을 사용하면 어떻게 되는가? default가 nullable=true면 충돌 발생할수도 있고 아무튼 위험한 점이 많지만 조심히만 사용하면 구버전 schema로 신버전 db를 조회해도 작동은 함. server_default와 default의 차이도 참고.